{
  "name": "linkly",
  "version": "1.0.0",
  "description": "Production-ready URL shortener with low latency and high throughput",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "db:migrate": "psql $DATABASE_URL -f db/schema.sql",
    "test": "node --test",
    "test:load": "hey -n 10000 -c 100"
  },
  "keywords": ["url-shortener", "base62", "redis", "postgresql"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "redis": "^4.6.12",
    "dotenv": "^16.3.1",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
